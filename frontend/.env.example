# Frontend Environment Variables
# Copy to .env.local for local development

# API base URL - leave empty to use Vite proxy (recommended for local dev)
VITE_API_URL=

# For production deployment on Azure App Service, set this to your backend URL:
# VITE_API_URL=https://lexiguard-api.azurewebsites.net
